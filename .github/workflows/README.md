# .github/workflows

CI: backend (uv, ruff, mypy --strict, pytest 3.11–3.13), frontend (pnpm, tsc, eslint, vitest), OpenAPI drift check, Playwright smoke on Linux; release: wheels, images, desktop bundles.
