# apps/desktop

Tauri 2 shell (later phase): bundles the backend as a `python-build-standalone` sidecar with wheels; auto-update. Phase A ships `uv tool install wise-workbench` with a CLI that starts the server and opens the browser.
