# `npm run build:live`: the build that the backend serves itself (`wise-workbench serve`).
# No backend URL: the application calls /api/v1 on its own origin; mocks are off.
VITE_API_URL=
VITE_USE_MOCKS=0
