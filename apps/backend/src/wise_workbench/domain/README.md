# apps/backend/src/wise_workbench/domain

Entities, value objects, state machines and invariants (Project, Dataset, NormVersion, Run, Finding, ValidationGate, Hypothesis, Action, Comparison). Pure Python; no I/O; never imports `wise`, SQLAlchemy or pandas.
