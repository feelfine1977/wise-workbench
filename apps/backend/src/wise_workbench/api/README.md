# apps/backend/src/wise_workbench/api

FastAPI routers, pydantic request/response schemas, RFC 9457 error model, SSE endpoints (job progress, assistant streams), auth (loopback token on desktop, OIDC on server).
