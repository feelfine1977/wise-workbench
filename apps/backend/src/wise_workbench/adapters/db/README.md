# apps/backend/src/wise_workbench/adapters/db

SQLAlchemy 2 models and repositories for metadata and review artefacts; SQLite (desktop) or Postgres (server); Alembic migrations in `apps/backend/alembic`.
